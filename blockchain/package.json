{
  "name": "blockbug-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Disabled legacy blockchain workspace retained for reference only.",
  "type": "module",
  "scripts": {
    "hardhat": "node -e \"console.log('Legacy blockchain runtime is disabled')\"",
    "chain": "node -e \"console.log('Legacy blockchain runtime is disabled')\"",
    "compile": "node -e \"console.log('Legacy blockchain runtime is disabled')\"",
    "deploy:local": "node -e \"console.log('Legacy blockchain runtime is disabled')\"",
    "service": "node -e \"console.log('Legacy blockchain runtime is disabled')\""
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.9",
    "dotenv": "^17.4.2",
    "ethers": "^6.16.0",
    "hardhat": "^3.4.1"
  }
}
